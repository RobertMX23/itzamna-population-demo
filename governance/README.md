# Governance

This directory contains versioned design and engineering rules used by CI and
by future coding sessions. The CSS specification is not model training; it is
project memory that can be retrieved as context before generating changes.

- `css-design-intelligence.yaml`: visual language, tokens, layout rules and QA contract.
